unbind r
bind r source-file ~/.tmux.conf

set -g prefix C-s
# act like vim
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R


# set -g @plugin 'maxpetretta/tmux-monokai-pro'
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'tmux-plugins/tmux-cpu'
# set -g @plugin 'tmux-plugins/tmux-battery'
# set -g @plugin 'mikejoh/tmux-k8s'
#
# set -g @monokai-left-pad ' ° '
# set -g @monokai-right-pad ' ° '
# set -g @monokai-left-pad '​'
# set -g @monokai-right-pad '​'
#
# set -g @monokai-show-right-sep 
# set -g @monokai-show-left-sep  
# set -g @monokai-transparent-powerline-bg true
# set -g @monokai-inverse-divider  █
# set -g @monokai-kubernetes-context-label "k8s"

set -g pane-border-lines single
set-option -g status-position top
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",*256col*:Tc"

set -g status-style bg=default

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
run '~/.tmux/plugins/tpm/tpm'

set -g status-style bg=default
set -g status-bg default
